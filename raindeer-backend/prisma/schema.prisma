generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Brand {
  id                 String   @id @default(uuid())
  email              String   @unique
  password           String
  linkedinPersonalConnected Boolean @default(false)
  linkedinCompanyConnected  Boolean @default(false)
  linkedinPersonalToken     String?
  linkedinCompanyToken      String?
  linkedinPersonId          String?
  logoUrl            String?
  brandName          String?
  category           String?
  website            String?
  product            String?
  usp                String?
  audience           String?
  audiencePainPoints String?
  audienceInterests  String?
  campaignObjective  String?
  toneFormal         Int      @default(50)
  toneSerious        Int      @default(50)
  toneMinimal        Int      @default(50)
  theme              String   @default("dark")
  websiteScrapedData String?  @db.Text
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  images             Image[]
  posts              Post[]
}

model Image {
  id          String   @id @default(uuid())
  brandId     String
  brand       Brand    @relation(fields: [brandId], references: [id])
  url         String
  imagekitId  String
  description String   @db.Text
  createdAt   DateTime @default(now())

  posts       Post[]
}

model Post {
  id              String   @id @default(uuid())
  brandId         String
  brand           Brand    @relation(fields: [brandId], references: [id])
  imageId         String?
  image           Image?   @relation(fields: [imageId], references: [id])
  date            DateTime
  content         String   @db.Text
  analysis        Json?
  type            String   @default("LinkedIn Post")
  status          String   @default("DRAFT") // DRAFT, PUBLISHED
  authorUrn       String?  // urn:li:person:ID or urn:li:organization:ID
  scheduledTime   String?      // "HH:MM" 24hr format e.g. "09:00"
  duration        Int?         @default(30)  // block duration in minutes
  imagePrompt     String?  @db.Text
  customImageUrl  String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
